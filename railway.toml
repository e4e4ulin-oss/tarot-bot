# Деплой на Railway: код берётся из GitHub, переменные задаются в веб-интерфейсе.
# Бот работает постоянным процессом (long polling), портов не занимает.
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m bot"
restartPolicyType = "ALWAYS"
